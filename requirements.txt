
pandas>=1.3.0
openpyxl>=3.0.0

# GUI Requirements
# tkinter is built-in with Python but may need system package:
# Ubuntu/Debian: sudo apt-get install python3-tk
# CentOS/RHEL: sudo yum install tkinter or sudo dnf install python3-tkinter
# macOS: brew install python-tk

# Development/Testing
# No additional packages required for console testing

# Note: sqlite3, datetime, calendar are built-in Python modules
=======
pandas>=1.5.0
openpyxl>=3.0.0
